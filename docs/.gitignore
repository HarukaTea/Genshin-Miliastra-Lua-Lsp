# VitePress 构建产物
.vitepress/dist
.vitepress/cache
.vitepress/.temp

# 依赖与本地缓存
node_modules
.npm-cache

# 由 npm run gen 生成，不入库以免产生无意义 diff
# （克隆后执行 `npm install && npm run gen` 即可重建）
api/
.vitepress/sidebar.json
